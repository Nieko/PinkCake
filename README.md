# PinkCake
Windows Desktop Background Slideshow / Shuffle

Periodically changes Windows Desktop Background to a random picture in a search path. Runs as a background application.

# Installation

The project contains a Visual Studio Installer project if you want to build a setup executable to run the install.
